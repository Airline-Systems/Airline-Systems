import mysql.connector

db= mysql.connector.connect(
    host="localhost",
    user="root",
    passwd= system.out.env('db.password'),
    database="testdatabase"
    )

mycursor = db.cursor()


























































































































































































































































































































